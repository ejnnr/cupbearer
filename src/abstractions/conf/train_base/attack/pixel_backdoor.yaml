# @package _global_

defaults:
  - /data@train_data:
    - pixel_backdoor
  - /data@val_data.clean:
    - pytorch
    - test
  - /data@val_data.backdoor:
    - pytorch
    - test
    - pixel_backdoor
  - _self_

train_data:
  transforms:
    pixel_backdoor:
      p_backdoor: 0.05

val_data:
  clean:
    name: ${train_data.name}
  backdoor:
    name: ${train_data.name}
