# @package _global_

experiment: ???
attack: ???
base_seed: ???
base_run: results/train_base/${get_grid_subdir:${hydra.job.override_dirname}}/${base_seed}

hydra:
  sweep:
    subdir: ${get_grid_subdir:${hydra.job.override_dirname}},seed=${base_seed}/${seed}